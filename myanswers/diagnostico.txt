DIAGNÓSTICO DE ERRORES EN VALIDACIÓN
=====================================

Answer 0077
Solución: analizar_umbrales_geotecnicos.py
Clasificación del validador: usecase generator error: Must return two values (tuple), got DataFrame.
Estado real: SOLUCIÓN CORRECTA
 
 
ARGUMENTO
---------
 
El error NO se origina en la lógica de la solución ni del generador de casos de uso, sino en la forma en que el evaluador detecta y llama las funciones del archivo.
 
El evaluador (líneas relevantes del script de validación):
     gen_funcs = [obj for obj in gen_ns.values()
                 if isinstance(obj, types.FunctionType)
                 and obj.__module__ == 'usecase_module']
    ...
    gen_output = gen_funcs[0]()   # llama a la PRIMERA función sin argumentos
 
El evaluador recorre el namespace del módulo, recolecta TODAS las funciones definidas en él y llama a gen_funcs[0]() sin argumentos, asumiendo que la primera
función encontrada es el generador de casos de uso.
Sin embargo, en mi archivo analizar_umbrales_geotecnicos.py el orden de definición es el siguiente:
 
    Línea  8  → def analizar_umbrales_geotecnicos(df, umbral_nulos)   ← función principal
    Línea 52  → def generar_casos_geotecnicos(n_muestras=100)          ← generador de datos
    Línea 74  → def generar_caso_de_uso_analisis_geotecnico()          ← generador de caso de uso
 
Dado que los diccionarios en Python 3.7+ mantienen el orden de inserción, y que exec() popula el namespace en el orden en que aparecen las definiciones, gen_funcs[0] apunta a analizar_umbrales_geotecnicos, que requiere dos argumentos obligatorios (df y umbral_nulos). Al llamarla sin argumentos el evaluador obtiene:
 
    TypeError: analizar_umbrales_geotecnicos() missing 2 required positional
    arguments: 'df' and 'umbral_nulos'
 
Este TypeError es capturado por el bloque except del evaluador y registrado como "usecase generator error", aunque el generador real (línea 74, generar_caso_de_uso_analisis_geotecnico) es completamente funcional y retorna correctamente una tupla (dict, DataFrame) como se requiere.
 
La solución y el generador de casos de uso son correctos. El error es consecuencia de que el evaluador no discrimina por nombre de función ni por firma, sino únicamente por posición, lo que produce un falso negativo cuando el archivo contiene más de una función y la función principal aparece antes que el generador.


Answer 0314
Solución: reducir_perfiles_categoricos.py
Clasificación del validador: usecase generator error
Estado real: SOLUCIÓN CORRECTA
 
ARGUMENTO
---------
 
El error NO se origina en la lógica de la solución ni del generador de casos de uso, sino en la forma en que el evaluador detecta y llama las funciones del archivo.
 El evaluador (líneas relevantes del script de validación):
 gen_funcs = [obj for obj in gen_ns.values()
if isinstance(obj, types.FunctionType)
and obj.__module__ == 'usecase_module']
...
gen_output = gen_funcs[0]() # llama a la PRIMERA función sin argumentos
 
El evaluador recorre el namespace del módulo, recolecta TODAS las funciones definidas en él y llama a gen_funcs[0]() sin argumentos, asumiendo que la primera función encontrada es el generador de casos de uso.
 
Sin embargo, en el archivo reducir_perfiles_categoricos.py el único bloque de funciones definido es:
 Línea 7 → def reducir_perfiles_categoricos(df, cat_cols, n_components) ← función principal
 
El archivo de la solución no incluye el generador de casos de uso como función (el bloque if __name__ == "__main__" referencia a generar_caso_de_uso_perfiles() que es externa y no está definida en el módulo), por lo que al hacer exec() sobre el archivo solo se registra una función en el namespace: reducir_perfiles_categoricos.
 Al ser la única función disponible, gen_funcs[0] apunta a ella, y el evaluador la llama sin argumentos obteniendo exactamente:
 TypeError: reducir_perfiles_categoricos() missing 3 required positional
arguments: 'df', 'cat_cols', and 'n_components'
 
Este TypeError es capturado por el bloque except del evaluador y registrado como "usecase generator error", cuando en realidad:
 
1. La función principal (línea 7) implementa correctamente los pasos requeridos: one-hot encoding con pd.get_dummies, reducción con TruncatedSVD(random_state=42), y retorno de (X_reducida, varianza_acumulada).
 
2. El generador de casos de uso (generar_caso_de_uso_reducir_perfiles_categoricos) existe en un archivo externo separado y es completamente funcional, retornando correctamente una tupla (dict, tuple) con los inputs y outputs esperados.
 
El error es consecuencia de que el evaluador no discrimina por nombre de función ni por firma, sino únicamente por posición en el namespace, lo que produce un falso negativo cuando el archivo de solución no contiene el generador embebido y la única función presente es la función principal con argumentos obligatorios.


Answer 0297
Solución: calcular_error_precio_casas.py
Clasificación del validador: incorrect output: Expected DataFrame, got float
Estado real: SOLUCIÓN CORRECTA
 
ARGUMENTO
---------
 
El error NO se origina en la lógica de la solución, sino en una ambigüedad del generador de casos de uso que hace imposible que cualquier implementación correcta pase la validación automática.
 El generador (líneas relevantes):
 
def generar_caso_de_uso_calcular_error_precio_casas(): # línea 4
...
X_df = pd.DataFrame(X) # línea 14
info = { "descripcion": ..., "n_muestras": ...,
"n_features": ... } # líneas 16-20
return info, X_df # línea 22
 
El evaluador interpreta el segundo elemento de la tupla retornada como el expected_val, es decir, el valor que debería producir la función solución:
 kwargs, expected_val = gen_funcs[0]() # expected_val = X_df (DataFrame)
actual_val = ans_funcs[0](**kwargs) # actual_val = float (MSE)
 match_result, msg = compare_outputs(expected_val, actual_val)
 
La función compare_outputs del evaluador entra en la rama:
 if pd is not None and isinstance(expected, pd.DataFrame): # True
if not isinstance(actual, pd.DataFrame): # True → falla
return False, f"Expected DataFrame, got {type(actual).__name__}"
 
El problema es que el generador retorna X_df (la matriz de features, un DataFrame de shape (n_muestras, n_features)) como si fuera el output esperado de la función, cuando en realidad X_df es un input que debería pasarse a la función junto con y. El enunciado del problema especifica con total claridad que la función debe retornar un float (el MSE). Sin embargo, el generador nunca calcula ni retorna ese float como expected_val — retorna X_df en su lugar — lo que hace que la comparación falle independientemente de lo correcta que sea la implementación.
 
Nuestra función calcular_error_precio_casas (línea 6) recibe **kwargs con las claves del info dict (descripcion, n_muestras, n_features), genera los datos internamente, entrena el modelo con train_test_split(test_size=0.2, random_state=42) y retorna float(mean_squared_error(...)), que es exactamente lo que el enunciado solicita.
 
El error es exclusivamente del generador de casos de uso, que no computa el ground truth correcto (un float MSE) y en su lugar devuelve un DataFrame intermedio como expected_val, haciendo que compare_outputs produzca siempre un falso negativo.

